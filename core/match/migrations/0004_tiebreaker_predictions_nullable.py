# Generated by Django 5.2.14 on 2026-06-12 19:19

from django.db import migrations, models


def zeros_to_null(apps, schema_editor):
    """Legacy rows used 0 as 'no guess' (the old defaults). NULL is the new
    'never picked the golden' marker — predictions are now mandatory at
    golden-pick time, so a real 0 prediction can only exist going forward."""
    TieBreaker = apps.get_model('core_match', 'TieBreaker')
    TieBreaker.objects.filter(owner_total=0).update(owner_total=None)
    TieBreaker.objects.filter(player_2_total=0).update(player_2_total=None)
    TieBreaker.objects.filter(total=0).update(total=None)


class Migration(migrations.Migration):

    dependencies = [
        ('core_match', '0003_phase4_phase5_invites_formats'),
    ]

    operations = [
        migrations.AlterField(
            model_name='tiebreaker',
            name='owner_total',
            field=models.IntegerField(blank=True, default=None, null=True),
        ),
        migrations.AlterField(
            model_name='tiebreaker',
            name='player_2_total',
            field=models.IntegerField(blank=True, default=None, null=True),
        ),
        migrations.AlterField(
            model_name='tiebreaker',
            name='total',
            field=models.IntegerField(blank=True, default=None, null=True),
        ),
        migrations.RunPython(zeros_to_null, migrations.RunPython.noop),
    ]
